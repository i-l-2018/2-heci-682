毕业论文格式修改：
1、如何将（四）改为（一）：将光标放到（四）处，然后右键，选择“重新开始于一”，便可将（四）改为（一）。
2、（1）在开始的菜单中找到样式，分别将标题一、标题二、标题三修改为要求的样式
   （2）再找到多级列表，定义新的多级列表，即将标题一，二，三定义成新的列表，定义完成过后选中新定义的列表。
   （3）然后将光标放到需要修改的标题前，选择其到底是哪个标题，然后其自动生成所需标题。

